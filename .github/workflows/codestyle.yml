name: Code Style 🔎

on:
  workflow_dispatch:
  workflow_call:

jobs:
  codestyle:
    name: Code Style 🔎
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
      - run: npm ci
      - run: npm run codestyle
